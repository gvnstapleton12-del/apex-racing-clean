export default function AppNavigation() {
  const tabs = ['Dashboard', 'Racecards', 'Results', 'Intelligence']

  return (
    <div className="sticky top-0 z-50 border-b border-white/10 bg-background/90 backdrop-blur-md">
      <div className="mx-auto flex max-w-6xl items-center justify-center gap-4 px-4 py-4">
        {tabs.map((tab) => (
          <button
            key={tab}
            className="
              flex
              h-12
              min-w-[160px]
              items-center
              justify-center
              rounded-xl
              border
              border-white/10
              bg-white/5
              px-6
              text-sm
              font-semibold
              tracking-wide
              text-white
              transition-all
              duration-200
              hover:-translate-y-0.5
              hover:border-white/20
              hover:bg-accent
              hover:shadow-lg
              active:scale-95
            "
          >
            {tab}
          </button>
        ))}
      </div>
    </div>
  )
}
